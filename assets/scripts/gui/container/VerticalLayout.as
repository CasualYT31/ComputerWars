/**
 * @file VerticalLayout.as
 * Defines the \c VerticalLayout class.
 */

/**
 * Represents a vertical layout in a GUI.
 */
shared class VerticalLayout : BoxLayoutRatios {
    /**
     * Create the vertical layout.
     */
    VerticalLayout() {
        super(VerticalLayoutEngineName);
    }
}

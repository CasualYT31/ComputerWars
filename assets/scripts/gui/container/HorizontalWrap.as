/**
 * @file HorizontalWrap.as
 * Defines the \c HorizontalWrap class.
 */

/**
 * Represents a horizontal wrap container in a GUI.
 */
shared class HorizontalWrap : BoxLayout {
    /**
     * Create the horizontal wrap container.
     */
    HorizontalWrap() {
        super(HorizontalWrapEngineName);
    }
}

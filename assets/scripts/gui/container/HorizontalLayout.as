/**
 * @file HorizontalLayout.as
 * Defines the \c HorizontalLayout class.
 */

/**
 * Represents a horizontal layout in a GUI.
 */
shared class HorizontalLayout : BoxLayoutRatios {
    /**
     * Create the horizontal layout.
     */
    HorizontalLayout() {
        super(HorizontalLayoutEngineName);
    }
}

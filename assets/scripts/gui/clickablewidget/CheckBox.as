/**
 * @file CheckBox.as
 * Defines the \c CheckBox class.
 */

/**
 * Represents a checkbox in a GUI.
 */
shared class CheckBox : RadioButton {
    /**
     * Create the check box.
     */
    CheckBox() {
        super(CheckBoxEngineName);
    }
}
